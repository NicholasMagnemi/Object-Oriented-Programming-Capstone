using System;

namespace Magnemi.ClassroomFullOfStudents.App04Design
{
    internal class InputValidation
    {
        public InputValidation() { }

        public bool stringToInt(string a)
        {
            // Validates that the input is a numeric number 
        }

        public bool containsNoNumbers(string a)
        {
            // Validates that the input does not contain numbers
        }
    }
}

